.git/
.github/
.ai/
docs/
*.md
.gitignore
Dockerfile
docker-compose.yml
Makefile
qodana.yaml
spotbugs-exclude.xml

# Build artifacts - exclude everything except JAR files for Docker build
# target/ is NOT excluded here because the Dockerfile needs the JAR
# This is an API spec project - the container just holds the artifact
# target/

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store

# Node
node_modules/
